program setverMyTask;

uses
  Forms,
  Unit1 in 'Unit1.pas' {Form1},
  coServer in 'coServer.pas' {coServerMyTask: TDataModule};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TcoServerMyTask, coServerMyTask);
  Application.Run;
end.
